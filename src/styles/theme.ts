import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#0f172a",
    },
    background: { paper: "#512DA8" },
    text: { primary: "#ffffff" },
  },
  typography: {
    body1: {
      // color
    },
  },
});
